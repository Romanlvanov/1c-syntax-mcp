# mcp 2.0.0 (2026-07-28) removed the low-level Server decorator API
# (@app.list_tools()/@app.call_tool()) that server.py uses — see docs/IMPROVEMENT_PLAN.md.
mcp>=1.10,<2
lxml>=5.0
rapidfuzz>=3.14
snowballstemmer>=3.1
# starlette/uvicorn/anyio уже приходят транзитивно через mcp, но webapp.py (HTTP-
# транспорт + веб-панель, Docker-развёртывание) импортирует их напрямую — фиксируем
# явно, чтобы понижение версии mcp не могло тихо утащить их вниз вместе с собой.
starlette>=0.40
uvicorn>=0.30
anyio>=4.0
